# To use project

1. first create virtual environment to install packages using python -m venv venv
2. install packages using install -r req.txt
3. run twitter_data.ipynb file in notebook or vscode

## install the following package by one of the following methods

1. pip install tweepy textblob wordcloud pandas numpy matplotlib

<!-- or  -->

2. pip install -r requirements.txt 
